class Piece {
  constructor(color, i, j) {
    this.color = color;
    this.i = i;
    this.j = j;
    this.icon = ' ';
  }

  // icon () {
  //
  // }

  moves () {
    var arr = [];
  }

  movePiece(startPos,endPos) {
    i += moves[0];
    j += moves[1];
  }

  validMoves(i,j){

  }
}

module.exports = Piece;
